import { compareAsc, format } from 'date-fns';

const Dom = (() => {

    const loadPage = () => {
        console.log("Hello");
    };
    const loadProjects = () => {

    };

    const loadTodos = () => {

    };

    const loadHeader = () => {
        
    };
    return {
        loadPage,
        loadProjects,
        loadTodos
    };

})();

export default Dom;